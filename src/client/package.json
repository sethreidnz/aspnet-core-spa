{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && npm run copyfiles",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "copyfiles": "copyfiles -u 1 ./build/**/* ./wwwroot"
  },
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {
    "copyfiles": "^2.0.0"
  }
}
